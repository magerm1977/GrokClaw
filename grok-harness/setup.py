"""Setup script for PyPI deployment.

For development, prefer: pip install -e .
The project uses pyproject.toml as the canonical config; this file
provides compatibility with tools that expect setup.py.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
